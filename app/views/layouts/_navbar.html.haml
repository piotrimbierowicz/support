.navbar.navbar-inverse.navbar-fixed-top{role: "navigation"}
  .container
    .navbar-header
      %button.navbar-toggle{"data-target" => ".navbar-collapse", "data-toggle" => "collapse", type: "button"}
        %span.sr-only Toggle navigation
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      %a.navbar-brand{href: root_path} Pulpit
    - if user_signed_in?
      .navbar-collapse.collapse
        .navbar-right
          %ul.nav.navbar-nav
            %li.dropdown
              %a.dropdown-toggle{"data-toggle" => "dropdown", href: "#"}
                = "Zalogowany jako: #{current_user.display_name}"
                %b.caret
              %ul.dropdown-menu
                %li
                  %a{href: profile_path} Moje konto
                - if current_user.role == User::Administrator
                  %li
                    = link_to 'Użytkownicy', users_path
                  %li
                    = link_to 'Dodaj projekt', new_project_path
                %li
                  = link_to 'Wyloguj', destroy_user_session_path, :method => 'delete'